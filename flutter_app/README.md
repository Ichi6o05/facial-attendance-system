# Flutter_app

## Primera ejecución del proyecto
flutter pub get
# Si vas a usar iOS/macOS:
# (opcional, pero recomendado si hay problemas) flutter clean
# cd ios && pod install
# cd .. # Volver al directorio raíz
flutter run